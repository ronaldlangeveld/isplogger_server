# Helm Chart for ISP Logger

## Installation
```sh
git clone https://github.com/ronaldlangeveld/isplogger_server
helm install --set network_id=<NETWORK ID> --set TZ=<TIMEZONE> isp-logger ./isplogger_server/charts/isp-logger
```
